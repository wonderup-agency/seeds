{
  "name": "seeds",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build-watch": "vite build --watch",
    "format": "prettier --write . && vite build"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lenis": "^1.3.1",
    "prettier": "^3.5.3"
  }
}
